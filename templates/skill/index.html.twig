{% extends 'base.html.twig' %}

{% block title %}Hello PlayerController!{% endblock %}

{% block body %}



<div class="jumbotron p-3 p-md-5 text-white rounded bg-dark" style="background-image:url(http://2.bp.blogspot.com/-D_irZ9f_5mU/UvD9NPUP4GI/AAAAAAAAFfg/pVUhhRuyDUM/s1600/wallpaper-fantasy-sepik.jpg); background-size: cover;background-position: center;
">
        <div class="col-md-6 px-0" style="padding: 50px;">
          <h1 class="display-4 font-italic">Page des compétences</h1>
          <p class="lead my-3">Sur cet page vous pourrez visualiser, editer, supprimer ou créer de nouveau monstre.</p>
          <!--<p class="lead mb-0"><a href="{{ path('monster_create', {id: 0}) }}" class="text-white font-weight-bold">Créer un monstre</a></p>-->
          <a class="btn btn-primary" href="{{ path('skill_create', {id: 0}) }}">Créer une compétence</a>
        </div>
      </div>
    <div class="container" style="">
    


{% for message in app.flashes('succes') %}
        <div class="alert alert-success">
            {{ message }}
        </div>
        {% endfor %}









            <div class="container" style="">
    <p class="lead my-3">Les monstres présents sont utilisable dans la création de quête, Touts les monstres validé pourrons être utilisé par touts les joueurs.
    Bien sur les monstres ne peuvent êtes supprimé et edité que par leur créateur ou bien des admins. </p>


    
            <div class="row" id="div3" style="display:none;">
            
                {% for skills in skill %}
                
                <!-- <div class="col-12 col-md"> -->
                <div class="d-flex  justify-content-center col-12 col-sm-12 col-md-6 col-lg-6 col-xl-4" style="padding: 30px;">

                        <div class="card " style="width: 18rem;padding: 10px;">
            <img class="card-img-top rounded img-fluid img-thumbnail" align="middle" style="width: 17rem;height: auto;margin: auto auto;" src="{{ skills.image }}" alt="Card image cap">
            <div class="card-body">
            <ul class="list-group list-group-flush">
                <li class="list-group-item list-group-item-action"><h5 class="card-title"> {{ skills.name }} </h5></li>
                <li class="list-group-item list-group-item-action  flex-column align-items-start">
                <div class="d-flex w-100 justify-content-between">
                <p class="card-text">{{ skills.description }}</p>
                <small>3 days ago</small>

                </div>
                </li>
                <li class="list-group-item list-group-item-action">
                
                <div class="row">
                    <div class="col-12">
                        <div class="list-group" id="list-tab" role="tablist">
                        <!--<a class="list-group-item list-group-item-action active" id="list-home-list{{ skills.id }}" data-toggle="list" href="#list-home{{ skills.id }}" role="tab" aria-controls="home{{ skills.id }}">Vie</a>-->
                        <a class="list-group-item list-group-item-action" id="list-profile-list{{ skills.id }}" data-toggle="list" href="#list-profile{{ skills.id }}" role="tab" aria-controls="profile{{ skills.id }}">vie</a>
                        <a class="list-group-item list-group-item-action" id="list-messages-list{{ skills.id }}" data-toggle="list" href="#list-messages{{ skills.id }}" role="tab" aria-controls="messages{{ skills.id }}">defense</a>
                        <a class="list-group-item list-group-item-action" id="list-settings-list{{ skills.id }}" data-toggle="list" href="#list-settings{{ skills.id }}" role="tab" aria-controls="settings{{ skills.id }}">Attaque</a>
                        </div>
                    </div>
                    
                    </div>
                
                </li>
                <li class="list-group-item list-group-item-action"><div class="col-12">
                        <div class="tab-content" id="nav-tabContent">
                        <!--<div class="tab-pane fade show active" id="list-home{{ skills.id }}" role="tabpanel" aria-labelledby="list-home-list{{ skills.id }}">description </div>-->
                        <div class="tab-pane fade" id="list-profile{{ skills.id }}" role="tabpanel" aria-labelledby="list-profile-list{{ skills.id }}">Nombre de point de vie du monstre {{ skills.skmana }}</div>
                        <div class="tab-pane fade" id="list-messages{{ skills.id }}" role="tabpanel" aria-labelledby="list-messages-list{{ skills.id }}">Defense du monstre {{ skills.skdef }}</div>
                        <div class="tab-pane fade" id="list-settings{{ skills.id }}" role="tabpanel" aria-labelledby="list-settings-list{{ skills.id }}">Degat d'attaque du monstre {{ skills.skdgt }}</div>
                        
                        </div>
                    </div></li>
                {% if skills.createur == user.id %}<li class="list-group-item list-group-item-action  d-flex justify-content-between align-items-center"><a href="{{ path('skill_edit', {id: skills.id}) }}" class="btn btn-primary">editer</a> {{ include('skill/_delete_form.html.twig') }}</li>{% endif %}
            </ul>
                
                
            
                
                
            </div>
            </div>
            </div>
            
                {% endfor %}

            </div>
            </div>



{% endblock %}
