{{ form_start(formRight) }}
    <div class="card" style="width: 16rem;padding: 10px;">
            {{ form_row(formRight.nameAsc) }}
            {{ form_row(formRight.nameDesc) }}
            {{ form_row(formRight.likeAsc) }}
            {{ form_row(formRight.likeDesc) }}
            {{ form_row(formRight.dateAsc) }}
            {{ form_row(formRight.dateDesc) }}
            {{ form_row(formRight.createdByMe) }}
            <button class="btn btn-primary" style="font-size: 1em;padding: 15px;">{% trans %}Search{% endtrans %}</button>
        </div>
{{ form_end(formRight) }}