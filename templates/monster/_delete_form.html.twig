<form method="post" action="{{ path('monster.delete', {id: monster.id}) }}" style= display:inline-block onsubmit="return confirm('Êtes vous vraiment sur ?')">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ monster.id) }}">
    <button class="btn btn-danger" onclick="return confirm('Are you sure you want to delete ?')">Delete</button>
</form>