{% extends 'base.html.twig' %}

{% block title %}Hello PlayerController!{% endblock %}

{% block body %}






<div class="jumbotron p-3 p-md-5 text-white rounded bg-dark" style="background-image:url(http://cdn.desktopwallpapers4.me/wallpapers/fantasy/2880x1800/2/17974-warrior-battling-the-giant-monsters-2880x1800-fantasy-wallpaper.jpg); background-size: cover;background-position: center;
">
        <div class="col-md-6 px-0" style="padding: 50px;">
          <h1 class="display-4 font-italic">Page des monstres</h1>
          <p class="lead my-3">Sur cet page vous pourrez visualiser, editer, supprimer ou créer de nouveau monstre.</p>
          <!--<p class="lead mb-0"><a href="{{ path('monster_create', {id: 0}) }}" class="text-white font-weight-bold">Créer un monstre</a></p>-->
          <a class="btn btn-primary" href="{{ path('monster_create', {id: 0}) }}">Créer un monstre</a>
        </div>
      </div>
    <div class="container" style="">
    <p class="lead my-3">Les monstres présents sont utilisable dans la création de quête, Touts les monstres validé pourrons être utilisé par touts les joueurs.
    Bien sur les monstres ne peuvent êtes supprimé et edité que par leur créateur ou bien des admins. </p>


{% for message in app.flashes('succes') %}
        <div class="alert alert-success">
            {{ message }}
        </div>
        {% endfor %}
    
            <div class="row"  id="div3" style="display:none;">
            
                {% for monsters in monster %}
                <!-- <div class="col-12 col-md"> -->
                <div class="d-flex  justify-content-center col-12 col-sm-12 col-md-6 col-lg-6 col-xl-4" style="padding: 30px;">

                        <div class="card " style="width: 18rem;padding: 10px;">
            <img class="card-img-top rounded img-fluid img-thumbnail" align="middle" style="width: 17rem;height: auto;margin: auto auto;" src="{{ monsters.image }}" alt="Card image cap">
            <div class="card-body">
            <ul class="list-group list-group-flush">
                <li class="list-group-item list-group-item-action"><h5 class="card-title"> {{ monsters.name }} </h5></li>
                <li class="list-group-item list-group-item-action  flex-column align-items-start">
                <div class="d-flex w-100 justify-content-between">
                <p class="card-text">{{ monsters.description }}</p>
                <small>3 days ago</small>

                </div>
                </li>
                <li class="list-group-item list-group-item-action">
                
                <div class="row">
                    <div class="col-12">
                        <div class="list-group" id="list-tab" role="tablist">
                        <!--<a class="list-group-item list-group-item-action active" id="list-home-list{{ monsters.id }}" data-toggle="list" href="#list-home{{ monsters.id }}" role="tab" aria-controls="home{{ monsters.id }}">Vie</a>-->
                        <a class="list-group-item list-group-item-action" id="list-profile-list{{ monsters.id }}" data-toggle="list" href="#list-profile{{ monsters.id }}" role="tab" aria-controls="profile{{ monsters.id }}">vie</a>
                        <a class="list-group-item list-group-item-action" id="list-messages-list{{ monsters.id }}" data-toggle="list" href="#list-messages{{ monsters.id }}" role="tab" aria-controls="messages{{ monsters.id }}">defense</a>
                        <a class="list-group-item list-group-item-action" id="list-settings-list{{ monsters.id }}" data-toggle="list" href="#list-settings{{ monsters.id }}" role="tab" aria-controls="settings{{ monsters.id }}">Attaque</a>
                        <a class="list-group-item list-group-item-action" id="list-autre-list{{ monsters.id }}" data-toggle="list" href="#list-autre{{ monsters.id }}" role="tab" aria-controls="autre{{ monsters.id }}">Compétences</a>
                        </div>
                    </div>
                    
                    </div>
                
                </li>
                <li class="list-group-item list-group-item-action"><div class="col-12">
                        <div class="tab-content" id="nav-tabContent">
                        <!--<div class="tab-pane fade show active" id="list-home{{ monsters.id }}" role="tabpanel" aria-labelledby="list-home-list{{ monsters.id }}">description </div>-->
                        <div class="tab-pane fade" id="list-profile{{ monsters.id }}" role="tabpanel" aria-labelledby="list-profile-list{{ monsters.id }}">Nombre de point de vie du monstre {{ monsters.hp }}</div>
                        <div class="tab-pane fade" id="list-messages{{ monsters.id }}" role="tabpanel" aria-labelledby="list-messages-list{{ monsters.id }}">Defense du monstre {{ monsters.def }}</div>
                        <div class="tab-pane fade" id="list-settings{{ monsters.id }}" role="tabpanel" aria-labelledby="list-settings-list{{ monsters.id }}">Degat d'attaque du monstre {{ monsters.atk }}</div>
                        <div class="tab-pane fade" id="list-autre{{ monsters.id }}" role="tabpanel" aria-labelledby="list-autre-list{{ monsters.id }}"><ul>{% for skill in monsters.skillbdd %}<li>{{ skill.name }}</li>{% endfor %}</ul></div>
                        </div>
                    </div></li>
                    {% if monsters.createur == user.id %}
                <li class="list-group-item list-group-item-action  d-flex justify-content-between align-items-center"><a href="{{ path('monster_edit', {id: monsters.id}) }}" class="btn btn-primary">editer</a> {{ include('monster/_delete_form.html.twig') }}</li>{% endif %}
            </ul>
                
                
            
                
                
            </div>
            </div>
            </div>
                {% endfor %}

            </div>
            </div>


    
    


{% endblock %}
